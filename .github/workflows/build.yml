name: Build

on:
  push:
    branches:
      - '**'
    tags:
      - '**'
  pull_request:
    branches:
      - '**'
  workflow_dispatch:

jobs:
  build:
    name: Build ${{ matrix.arch }} ${{ matrix.manifest_mode == 'ON' && 'with vcpkg''s manifest mode' || 'without vcpkg''s manifest mode' }}
    runs-on: windows-latest
    strategy:
      matrix:
        manifest_mode: ['ON', 'OFF']
        arch: ['x64', 'x86']
        include:
          - arch: x64
            cmake_arch: x64
            triplet: x64-windows-static
            output_name: fische-x64.exe
          - arch: x86
            cmake_arch: Win32
            triplet: x86-windows-static
            output_name: fische-x86.exe

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Cache vcpkg packages
        uses: actions/cache@v5
        with:
          # Cache both global and manifest paths
          path: |
            C:\vcpkg\installed
            vcpkg_installed
          key: vcpkg-${{ matrix.triplet }}-manifest-${{ matrix.manifest_mode }}-${{ hashFiles('vcpkg.json', 'vcpkg-configuration.json') }}
          restore-keys: vcpkg-${{ matrix.triplet }}-manifest-${{ matrix.manifest_mode }}-

      - name: Update vcpkg baseline
        run: vcpkg x-update-baseline

      - name: Install dependencies via vcpkg (Manifest Mode)
        if: matrix.manifest_mode == 'ON'
        run: vcpkg install --triplet ${{ matrix.triplet }}

      - name: Install dependencies via vcpkg (Classic Mode)
        if: matrix.manifest_mode == 'OFF'
        run: |
          # 1. Parse dependencies from vcpkg.json
          $manifest = Get-Content vcpkg.json -Raw | ConvertFrom-Json
          $deps = $manifest.dependencies | ForEach-Object { if ($_.name) { $_.name } else { $_ } }
          
          # 2. Rename the manifest so CMake/vcpkg don't implicitly fallback to manifest mode
          Rename-Item vcpkg.json vcpkg.json.bak
          
          # 3. Install parsed dependencies globally (Classic Mode)
          if ($deps) {
            $depList = $deps -join " "
            Invoke-Expression "vcpkg install $depList --triplet ${{ matrix.triplet }}"
          }
        shell: pwsh

      - name: Configure CMake
        run: >
          cmake -B build-${{ matrix.arch }}
          -A ${{ matrix.cmake_arch }}
          -DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake 
          -DVCPKG_TARGET_TRIPLET=${{ matrix.triplet }}
          -DVCPKG_MANIFEST_MODE=${{ matrix.manifest_mode }}

      - name: Build
        run: cmake --build build-${{ matrix.arch }} --config Release

      - name: Assemble artifact
        run: |
          mkdir dist
          copy build-${{ matrix.arch }}\Release\fische.exe dist\${{ matrix.output_name }}
        shell: cmd

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: fische-windows-${{ matrix.arch }}-manifest-${{ matrix.manifest_mode }}
          path: dist/

      # - name: Upload release asset (on tag)
      #   if: startsWith(github.ref, 'refs/tags/')
      #   uses: softprops/action-gh-release@v2
      #   with:
      #     files: fische-windows-bin\*
